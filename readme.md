# Студент Цыкалов
## Вопросы которые до конца не понимаю:
1. В домашнем задании сказано требуется создать модуль hw01-gradle, но если мы создаем именно модуль в котором есть
доступ только к определенному пакету, то как необходимо обозвать имя модуля в файле "module-info.java", в данном файле
применение тире запрещено. Возможно выполнил не верно, применил подчеркивание.
2. Сделал два варианта формирования толстого jar файла (с помощью функции jar и с помощью shadowJar), понимаю что 
достаточно одного варианта, но возник вопрос в правильности введения строки 
duplicatesStrategy = DuplicatesStrategy.EXCLUDE 
при формировании с помощью функции jar. Почему при формировании с помощью функции Jar мы должны указывать стратегию
сборки, а при применении shadowJar нет?
3. И третий вопрос требуется ли вставлять в файл gradle.properties строку:
   org.gradle.configuration-cache=true
С какой целью ее вставил. Первоначально когда собирал проект после компиляции выводилось сообщение о том что не 
подключен кэш. По данной причине его установил, но в правильности решения не уверен.
4. Не уверен в правильности проведения логирования и тестирования, уже понял что данная тема будет далее, но в проект
уже вставил удалять не стал. 
### И не уверен все ли файлы необходимо было вставлять в Git. С Gradle работаю в первые по этой причине возникло столько вопросов.
Понимаю что READMI служит руководством по проекту. Но так как вопросов много решил написать здесь. Перед слиянием ветки
данные вопросы удалю.