# Студент Цыкалов
## Домашнии задания:
### 1. Условия задания: - Проект gradle с модульной структурой
Цель:
научиться создавать проект Gradle, подготовиться к выполнению домашних заданий.


Описание/Пошаговая инструкция выполнения домашнего задания:
1.1 Создайте аккаунт на github.com (если еще нет).

1.2 Создайте репозиторий для домашних работ.

1.3 Сделайте checkout репозитория на свой компьютер.

1.4 Создайте локальный бранч hw01-gradle.

1.5 Создать проект gradle. В проект добавьте последнюю версию зависимости
com.google.guava guava

1.6 Создайте модуль hw01-gradle.

1.7 В модуле сделайте класс HelloOtus.

1.8 В этом классе сделайте вызов какого-нибудь метода из guava.

1.9 Создайте "толстый-jar".

1.10 Убедитесь, что "толстый-jar" запускается.

1.11 Сделайте pull-request в gitHub.

1.12 Ссылку на PR отправьте на проверку (личный кабинет, чат с преподавателем).
### Результат: проект выполнен, имя модуля: hw01-gradle.

### 2. Условия задания: - Применение коллекций
Цель:
попрактиковаться в использовании различных аспектов коллекций и обобщений.


Описание/Пошаговая инструкция выполнения домашнего задания:
2.1 Написать метод, который меняет два элемента массива местами (массив может быть любого ссылочного типа).

2.2 Написать метод, который преобразует массив в ArrayList.

2.3 Создать массив с набором слов (10-20 слов, должны встречаться повторяющиеся). Найти и вывести список уникальных слов, из которых состоит массив (дубликаты не считаем). Посчитать, сколько раз встречается каждое слово.
### Результат: проект выполнен, имя модуля: hw02_gradle.

### 3. Условия задания: - Свой тестовый фреймворк
Цель:
научиться работать с reflection и аннотациями и понять принцип работы фреймворка junit.

Описание/Пошаговая инструкция выполнения домашнего задания:

3.1 Написать свой тестовый фреймворк.

3.2 Поддержать свои аннотации @Test, @Before, @After.

3.3 Запускать вызовом статического метода с именем класса с тестами.

Т.е надо сделать:

1. Создать три аннотации - @Test, @Before, @After.
2. Создать класс-тест, в котором будут методы, отмеченные аннотациями.
3. Создать "запускалку теста". На вход она должна получать имя класса с тестами, в котором следует найти и запустить методы отмеченные аннотациями и пункта 1
Алгоритм запуска должен быть следующий::
метод(ы) Before
текущий метод Test
метод(ы) After
для каждой такой "тройки" надо создать СВОЙ экземпляр класса-теста.
4. Исключение в одном тесте не должно прерывать весь процесс тестирования.
5. На основании возникших во время тестирования исключений вывести статистику выполнения тестов (сколько прошло успешно, сколько упало, сколько было всего).
6. "Запускалка теста" не должна иметь состояние, но при этом весь функционал должен быть разбит на приватные методы. Надо придумать, как передавать информацию между методами.
### Результат: проект выполнен, имя модуля: hw03_gradle.

### 4. Условия задания: -Определение нужного размера хипа
Цель:
понять на примере простого приложения, какое влияние оказывают сборщики мусора.

Описание/Пошаговая инструкция выполнения домашнего задания:

Есть готовое приложение (модуль homework)- использую hw02_gradle

1. Запустите его с размером хипа 256 Мб и посмотрите в логе время выполнения.
При запуске с командной строки файл с логами расположен в пакете: \hw04_gradle\build\libs\hw04_gradle\logis\application_log.log

2. Увеличьте размер хипа до 2Гб, замерьте время выполнения запусков, записывайте в таблицу.
файл с логами находится в пакете:
   hw04_gradle\build\libs\hw04_gradle\logis\application_log.log

3. Определите оптимальный размер хипа, т.е. размер, превышение которого не приводит к сокращению времени выполнения приложения.
оптимальным размером используемой памяти составляет 1024 МБайта.

4. Оптимизируйте работу приложения. Т.е. не меняя логики работы (но изменяя код), сделайте так, чтобы приложение работало быстро с минимальным хипом.
оптимизированное приложение находится в пакете проекта: 
hw04_gradle\src\main\java\org\example\module_hw02_modified

5. Повторите измерения времени выполнения программы для тех же значений размера хипа.
данные представлены в том же пакете расположенном по адресу:
   hw04_gradle\build\libs\hw04_gradle\logis\application_log.log

Примечание: 
1. Если в приложении требуется вывести в консоль логи информации исследуемого модуля, то в файле logback.properties произвести замену строки

logger.info.module=OFF на строку
logger.info.module=DEBUG
- В модуле файл hw02_gradle.jar используется для демонстрации вызова метода main() через рефлексию.
- В пакете module_hw02_modified расположен модифицированный модуль hw02_gradle.
- Тесты jmh расположены в соответствующей папке.
- Если мы запускаем метод main из IDEA логи приложения сохраняются в hw04_gradle.logis.application_log.log.
- Если мы вызываем hw04_gradle.jar соответственно с указанием выделеной памяти, то результаты записываются в файл 
hw04_gradle.logis.application_log.log расположенный в пакете build.libs
### Результат: проект выполнен, имя модуля: hw04_gradle.

### 5. Условия задания:
Цель: 
понять как реализуется AOP и какие для этого есть технические средства.

Описание/Пошаговая инструкция выполнения домашнего задания:
1. Разработайте такой функционал:
   метод класса можно пометить самодельной аннотацией @Log.
2. Обратите внимание: явного вызова логирования быть не должно.
   Первонадально, понял условие, что данное условие распространяется только на сам проект. 
Поэтому сделал два варианта вывода логов:
- через логирование информации в LogInterceptor - метод outPrintLogger(target, method,args);
- вариант вывода информации через System.out.println() - метод outPrintEnglish(target, method, args);
В зависимости от вида вывода информации работаем либо одним методом, либо вторым (закоментированным).

P.S.
Выбирайте реализацию с ASM, если действительно этого хотите и уверены в своих силах.
Понимаю, что реализацию с библиотекой ASM в поставленные сроки реализовать не смогу, на данный момент очень большие трудности 
возникают с выводом параметров методов, но по этому вопросу работаю, думаю в ближайшие две недели разберусь. 
На данный момент реализовал поставленную задачу с помощью библиотеки Byte Buddy (она очень помогла разобраться с
основными принципами работы с библиотекой ASM, но она для понимания мне пока полностью не под силу.)

Примечание:
1. Тестируемые классы расположены в пакете - application.
2. Приложение агент соответственно в пакете - agent.
3. Сборка проекта application организована с помощью команды gradle jar (получаем application.jar)
4. Сборка проекта agent организована с помощью команды gradle shadowJar (получаем agent.jar)
5. Запуск приложения организован в двух вариантах:
- с помощью командной строки java -javaagent:agent.jar -jar application.jar 
- второй вариант собираем агента с помощью команды gradle shadowJar, и далее набираем команду gradle runAgent 
### Результат: проект выполнен, имя модуля: hw05_gradle.

### 6. Условия задания: Эмулятор банкомата

Цель:
применить на практике принципы SOLID.

Описание/Пошаговая инструкция выполнения домашнего задания:
1.1 Написать эмулятор АТМ (банкомата).

Объект класса АТМ должен уметь:
- принимать банкноты разных номиналов (на каждый номинал должна быть своя ячейка) - метод класса CashMoneyATM - public int acceptDeposit(CashMoneyPerson cashMoneyPerson).
- выдавать запрошенную сумму минимальным количеством банкнот или ошибку, если сумму нельзя выдать - метод класса CashMoneyATM - public Map<String, Integer> withdrawAndUpdateAfterIssue(int cash).
- выдавать сумму остатка денежных средств - метод класса CashMoneyATM - public int amountOfCash() (метод расположен в абстрактных классах CashMoneyRubleAbstract и CashMoneyDollarAbstract в зависимости
  от того, какой класс является родителем, подключается метод с соответствующего класса. В моем случае классом родителем является класс CashMoneyRubleAbstract).

Примечание: в данном приложении есть возможность масштабировать систему под другие валюты (рубли, доллары, можно добавить дополнительно абстрактные классы под дополнительую валюту).
Если по текущему заданию замечаний не будет, в дальнейшем попытаюсь реализовать паттерн «Фабрика», позволяющий создавать банкоматы с заданной валютой (например, рубли, доллары) без привязки к конкретному классу.
### Проект расположен в модуле: hw06_gradle.
1.2 Второй вариант исполнения эмулятора банкомата.
Отличается от первого варианта в моделях валют используем не поля с номиналами валют, а мапу.
И в данном варианте реализована возможность работы банкомата с различными типами валют.

### 7. Условия задания: Обработчик сообщений

Цель: применить на практике шаблоны проектирования.
Описание/Пошаговая инструкция выполнения домашнего задания:
Реализовать to do:
1. Добавить поля field11 - field13 (для field13 используйте класс ObjectForMessage): внесены изменения в класс Message.
2. Сделать процессор, который поменяет местами значения field11 и field12: добавлен новый класс с реализацией процессора - ChangeValuesOfField11And12.
3. Сделать процессор, который будет выбрасывать исключение в четную секунду (сделайте тест с гарантированным результатом)
Секунда должна определяться во время выполнения: добавлен новый класс с реализацией процессора - ThrowOnExceptionEvenSecond.
Тест - важная часть задания: реализован в классе - ThrowOnExceptionEvenSecondTest.
4. Сделать Listener для ведения истории (подумайте, как сделать, чтобы сообщения не портились)
Уже есть заготовка - класс HistoryListener, надо сделать его реализацию
Для него уже есть тест, убедитесь, что тест проходит.
 Примечание: в данном домашнем задании в заготовке homework, были реализованы пустые файлы forHomeWork, я так понял, что данные файлы предназначены для хранения логов (возможно ошибаюсь).
Но для хранения логов выбрал другой пакет logs, и в нем два пакета listener и processor, соответственно первый для хранения слушателей, второй для результата работы процессоров.

### Результат: проект выполнен, имя модуля: hw07_gradle.

### 8. Условия задания: Обработчик json-ов

Цель: научиться обрабатывать json, научиться работать с файлами.
Описание/Пошаговая инструкция выполнения домашнего задания:
Некая система:
1. принимает входящий json файл;
2. обрабатывает данные из файла;
3. формирует ответный файл;
4. нужно реализовать недостающий функционал.
Примечание: дополнительно добавлен Demo класс, в нем просто создается список json строк и они обрабатываются согласно алгоритма.
Результат хранится в пакете demo в корне проекта.